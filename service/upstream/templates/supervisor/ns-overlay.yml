#! This file is intentionally empty.
#! The argo-workflows.namespace helper in _helpers.tpl already injects .Values.namespace
#! for all namespaced resources in the chart. A broader ytt overlay is not needed and
#! causes duplicate Roles when chart RBAC iterates over workflowNamespaces.
